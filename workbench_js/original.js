function main(input) {
    let [nm, a] = input.split('\n');
    let [n, m] = nm.split(' ');

    let ans = 0;
    console.log(ans);
    return 0;
}

main(require('fs').readFileSync('/dev/stdin', 'utf8'));
